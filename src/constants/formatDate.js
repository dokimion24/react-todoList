export const formattedDate = (todoDate) => {
  // const date = new Date(todoDate);
  // const year =
  // const month = String(date.getMonth() + 1).padStart(2, '0');
  // const today = String(date.getDate()).padStart(2, '0');

  // return `${month}/${today} ${hour}:${min}`;
};
